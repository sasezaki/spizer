<?php

require_once 'Spizer/Handler/Abstract.php';

class Kumo_Handler_DigginScraper
{
    public function __construct(array $config = array())
    {
        if (!isset($config['expression']) and !isset())
    }
}
